# HR Core — D1 Schema Manager

هذا المشروع يستخدم طبقة Schema Manager داخل Cloudflare Worker للحفاظ على توافق قاعدة D1 مع التطبيق.

## ماذا يحدث عند الطلب

1. Worker يستدعي `ensureSchema()` قبل أي API.
2. يتم إنشاء جداول التشغيل المفقودة باستخدام `CREATE TABLE IF NOT EXISTS`.
3. يتم فحص الجداول القديمة عبر `PRAGMA table_info`.
4. إذا كان عمود مطلوب غير موجود، تتم إضافته تلقائيًا فقط إذا كانت الإضافة آمنة.
5. تتم مزامنة Permission Catalog مع جدول الصلاحيات القديم عبر `permission_map` بدل افتراض أن نوع المعرّف واحد.
6. يتم تسجيل أي مشكلة بنيوية في `schema_issues` بدل إخفائها.
7. يتم تسجيل نسخة المخطط في `schema_migrations`.
8. لا يتم حذف جدول أو عمود أو بيانات أعمال تلقائيًا.

## قاعدة الأمان

الإضافة التلقائية مسموحة. التغيير التدميري غير مسموح تلقائيًا.

بالتالي لا يوجد منطق من نوع:

- DROP TABLE
- DROP COLUMN
- حذف بيانات لمجرد أنها غير مستخدمة
- تغيير نوع عمود قائم تلقائيًا
- إعادة بناء جدول قائم أثناء تسجيل الدخول

أي تغيير تدميري مستقبلي يجب أن يكون Migration صريحًا ومراجعًا.

## فحص الحالة

`GET /api/health` يعرض:

- اتصال D1
- `schemaReady`
- رقم نسخة المخطط
- عدد المشاكل البنيوية المفتوحة
- أسماء الجداول الموجودة

كما توجد صيانة النظام عبر:

`GET /api/system/maintenance`

و:

`POST /api/system/maintenance`

الأمر الثاني ينفذ تنظيفًا محدودًا للسجلات اليتيمة المعروفة فقط.

## الصلاحيات القديمة والجديدة

التطبيق يستخدم مفاتيح مستقرة مثل:

`employees.view`

`transactions.approve`

`payroll.view`

`system.maintenance`

وفي حال كانت قاعدة البيانات القديمة تستخدم أرقامًا في `permissions.id` و`role_permissions.permission_id`، يقوم النظام بإنشاء `permission_map` ويحافظ على البيانات القديمة بدل محاولة إدخال نص في عمود رقمي.

## ما الذي لا يمكن ضمانه تلقائيًا

لا يمكن لأي نظام أن يضمن أن أي SQL جديد خاطئ سيعمل دون خطأ. ما يتم ضمانه هنا هو أن تغيّر المخطط المتوقع للتطبيق لا يتحول إلى مفاجأة عند تسجيل الدخول أو أول API، وأن المفقود المعروف يتم إنشاؤه أو تسجيله بوضوح.
