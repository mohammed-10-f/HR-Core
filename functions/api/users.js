import {body,fail,json,requirePermission,hashPassword,id,audit} from './_core.js';
export async function onRequestGet({request,env}){const a=await requirePermission(request,env,'users.view');if(a.error)return a.error;const rows=await env.DB.prepare(`SELECT u.id,u.username,u.display_name,u.role_id,u.employee_id,u.active,u.created_at,u.updated_at,r.name role_name,e.name employee_name FROM users u JOIN roles r ON r.id=u.role_id LEFT JOIN employees e ON e.id=u.employee_id WHERE u.company_id=? ORDER BY u.display_name`).bind(a.user.company_id).all();return json({ok:true,users:rows.results||[]})}
export async function onRequestPost({request,env}){const a=await requirePermission(request,env,'users.create');if(a.error)return a.error;const b=await body(request);if(!b.username||!b.password||!b.display_name||!b.role_id)return fail('الاسم واسم المستخدم وكلمة المرور والدور مطلوبة.');const exists=await env.DB.prepare('SELECT id FROM users WHERE username=?').bind(b.username).first();if(exists)return fail('اسم المستخدم مستخدم بالفعل.',409);const uid=id('usr'),ph=await hashPassword(String(b.password));await env.DB.prepare(`INSERT INTO users(id,company_id,username,display_name,pin_hash,role_id,employee_id,active) VALUES(?,?,?,?,?,?,?,1)`).bind(uid,a.user.company_id,b.username,b.display_name,ph,b.role_id,b.employee_id||null).run();await audit(env,a.user,'create','user',uid,{username:b.username,role_id:b.role_id});return json({ok:true,id:uid},201)}
export async function onRequestPatch({request,env}){const a=await requirePermission(request,env,'users.edit');if(a.error)return a.error;const b=await body(request);if(!b.id)return fail('معرف المستخدم مطلوب.');const fields=[];const vals=[];for(const k of ['display_name','role_id','employee_id','active'])if(b[k]!==undefined){fields.push(`${k}=?`);vals.push(b[k]);}if(b.password){fields.push('pin_hash=?');vals.push(await hashPassword(String(b.password)));}if(!fields.length)return fail('لا توجد تغييرات.');vals.push(b.id,a.user.company_id);await env.DB.prepare(`UPDATE users SET ${fields.join(',')},updated_at=CURRENT_TIMESTAMP WHERE id=? AND company_id=?`).bind(...vals).run();await audit(env,a.user,'update','user',b.id,{fields:fields.map(x=>x.split('=')[0])});return json({ok:true})}
